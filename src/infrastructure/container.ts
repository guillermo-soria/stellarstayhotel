import { InMemoryRoomRepository } from "./repositories/in-memory-room.repository";import { InMemoryReservationRepository } from "./repositories/in-memory-reservation.repository";import { PricingEngine } from "../domain/services/pricing-engine";import { GetAvailableRooms } from "../application/use-cases/get-available-rooms";import { CreateReservation } from "../application/use-cases/create-reservation";// Shared instances (singletons for in-memory repositories)export const reservationRepo = new InMemoryReservationRepository();export const roomRepo = new InMemoryRoomRepository(reservationRepo); // Inject reservation repoexport const pricingEngine = new PricingEngine();// Use cases with shared dependenciesexport const getAvailableRooms = new GetAvailableRooms(roomRepo, pricingEngine);export const createReservation = new CreateReservation(reservationRepo, roomRepo, pricingEngine, reservationRepo);